<h1>Create a new City</h1>

<%= form_for(@city) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :state %>
  <%= f.text_field :state %>

  <%= f.submit "Create a new city", class: "btn btn-inverse"%>
<% end %>
